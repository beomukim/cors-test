management:
    endpoints:
        web:
            cors:
                allowed-origins: http//localhost:3000
                allowed-methods: GET,POST

spring:
    servlet:
        csrf:
            enabled: false
